[project]
name = "hermes-trading"
version = "0.1.0"
description = "Self-improving paper trading agent with Hermes reflection"
requires-python = ">=3.10"

dependencies = [
    "pyyaml>=6.0",
    "httpx>=0.27",
    "anthropic>=0.25.0",
]

[tool.uv]
dev-dependencies = []
